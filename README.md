This project follows a FastAPI tutorial to build basic CRUD (Create, Read, Update, Delete) endpoints.
It simulates a simplified social media backend at a surface level.

The application generates random post IDs during the Create operation and stores posts in memory.
The Read endpoint allows retrieval of posts by passing the correct post ID as a path parameter.
